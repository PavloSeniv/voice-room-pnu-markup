Створення нового проекту на основі цієї збірки:
1. Папки #src, файли gulpfile.js and package.json;
2. Перекинути ці папки та файли до нового проекту;
3. В терміналі у проекті ввести команду npm i;
(Вона завантажить всі плагіни, які записані у package.json).
Почати роботу)!!!

Beg:
1) Бути уважним з тегом img(щоб все було в одному рядку);
2) Присутній баг з додаванням додаткової крапочки через webp-html and webp-css(Ніби вже немає);
3) Не задавати ім'я файлу з _*js у робочій версії.
4) normalize.css - scss свариться на коментарії у коді(виправлена версія у docx, виправити)
